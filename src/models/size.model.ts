import { LetterSize , NumberSize } from '../types';

/**
 * {@link Size} handles size for {@link Item}.
 */
export class Size {
    Country: string; //// Change to type later on

    Size: LetterSize | NumberSize; //// item size
}